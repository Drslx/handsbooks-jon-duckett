/* Operador (AND = &&) Permite encadear duas ou mais expressões */
/* Usamos operador condicional if e else */
let sexo = "F";
let idade = 24;

/* Sexo igual "M" E Idade maior o ou igual a 23 */
if (sexo == "M" && idade >= 23) {
  console.log("Permitido");
} else {
  console.log("Nao Permitido");
}
