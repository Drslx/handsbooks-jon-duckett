/* setup = () => {
  let textInput = document.getElementById("username");
  textInput.focus();
};
 */
function setup() {
  // body
  let textInput = document.getElementById("username");
  textInput.focus();
} 

window.addEventListener("load", setup, false);
